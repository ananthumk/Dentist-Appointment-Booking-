{
  "name": "backend",
  "version": "1.0.0",
 
  "scripts": {
    "dev:backend": "nodemon server.js",
    "build:frontend": "cd ../src && npm run build && cd ../backend && rm -rf public && mkdir -p public && cp -r ../src/build/* ./public/",
    "start": "node server.js"
  }

,
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "mongoose": "^8.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
